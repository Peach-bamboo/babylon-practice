// 自定义类型存储
export type Keys = {
    shift: boolean,
    w: boolean,
    s: boolean,
    a: boolean,
    d: boolean,
}
export type WallSizeType = {
    height: number,
    width: number,
    depth: number
}